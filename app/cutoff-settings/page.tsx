import CutoffSettingsModule from "@/modules/cutoff-settings";

/**
 * Root cutoff settings page.
 * Standardized URL structure: /cutoff-settings
 */
export default function CutoffSettingsRootPage() {
  return <CutoffSettingsModule />;
}
